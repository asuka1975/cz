// TEST: if式の分岐型不一致エラー
// EXPECT: compile-error
// DESCRIPTION: if式の両分岐の型が一致しない場合にコンパイルエラーになることを検証する
// NOTE: MS1ではi32しか型がないため、このテストは将来の型拡張時に意味を持つ。
//       MS1では構文的に不正な値を分岐に置くことでエラーを検証する。

fn main() -> i32 {
    // 末尾式がないブロックを値として使用しようとするエラー
    let x: i32 = if true { 1 } else { };
    x
}
